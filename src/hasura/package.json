{
  "name": "@shepherdorg/hasura-types",
  "version": "3.0.1",
  "main": "dist/apiTypes.ts",
  "types": "dist/apiTypes.d.ts",
  "license": "MIT",
  "scripts": {
    "gen-types": "npx graphql-cli get-schema --project shepherd && graphql-schema-typescript generate-ts schema.graphql --output src/apiTypes.ts",
    "build": "rm -rf dist/ && tsc -p .",
    "prepublishOnly": "npm run build"
  },
  "files": [
    "dist"
  ],
  "dependencies": {
    "@pongsatt/graphql-schema-typescript": "^1.0.25",
    "graphql": "^0.13.0",
    "typescript": "^3.6.4"
  }
}
