{
  "name": "@shepherdorg/hasura-types",
  "version": "3.0.0",
  "main": "dist/apiTypes.ts",
  "types": "dist/apiTypes.d.ts",
  "license": "MIT",
  "scripts": {
    "gen-types": "graphql get-schema --project shepherd && graphql-schema-typescript generate-ts schema.graphql --output src/apiTypes.ts",
    "build": "rm -rf dist/ && tsc -p .",
    "prepublishOnly": "npm run build"
  },
  "files": [
    "dist"
  ],
  "dependencies": {
    "@pongsatt/graphql-schema-typescript": "^1.0.25",
    "graphql": "^14.5.8",
    "graphql-cli": "^3.0.14",
    "typescript": "^3.6.4"
  }
}
